spring:
    cloud:
        config:
            server:
                git:
                    uri: file://${user.home}/projetos/microservices/
                    search-paths:
                        - "config-properties/{application}/{profile}"