server:
    port: ${port:8082}

spring:
    application:
        name: mail
    cloud:
        config:
            allowOverride: true
            overrideNone: false
            discovery:
                enabled: true
                service-id: config-server